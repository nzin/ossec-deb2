ossec-hids for Debian
---------------------

This package currently uses some patches that modify upstream sources to be
built and installed in Debian systems, the infrastructure is in place to add
these patches at built using quilt.

Changes to the sources should be done using quilt, and patches should be stored at debian/patches.

